Skip to content
Navigation Menu
HuachaDev
/
node-algoritmo

Type / to search

Code
Issues
Pull requests
Actions
Projects
Wiki
Security
Insights
Settings
Editing README in node-algoritmo
Breadcrumbsnode-algoritmo
/
README
in
master

Edit

Preview
 @@ -1,33 +0,0 @@
# API de Filtrado de URLs

## Descripción del Proyecto

La API de Filtrado de URLs permite procesar un archivo de texto con URLs y filtrar aquellas que pertenecen a dominios que contienen la palabra "shop" y que terminan con la extensión ".html". Esta API devuelve un JSON con el total de URLs válidas y una lista de estas.

## Tecnologías Utilizadas

- **Node.js:** 
- **Express:** 
- **Multer:** 
- **File System (fs):**

## Instrucciones para Ejecutar el Proyecto

1. **Clonar el repositorio**:
    ```bash

    ```

2. **Instalar las dependencias**:
    ```bash
    npm install
    ```

3. **Levantar el servidor local**:
    ```bash
    node src/server.js
    ```

El servidor estará disponible en `http://localhost:3000`.


Editing node-algoritmo/README at master · HuachaDev/node-algoritmo
